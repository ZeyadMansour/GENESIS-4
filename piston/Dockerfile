# GENESIS-4 Piston Sandbox
# Isolated code execution engine — deployed on Render free tier
FROM ghcr.io/engineer-man/piston:latest

# Expose the Piston API
EXPOSE 2000

# Start the Piston server
CMD ["/piston/piston", "--port", "2000"]
